/*
function setCookie(cname, cvalue, exdays) {
    var d = new Date();
    d.setTime(d.getTime() + (exdays*24*60*60*1000));
    var expires = "expires="+ d.toUTCString();
    document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
}



Una función para obtener una cookie
Luego, creamos una función que devuelve el valor de una cookie especificada:

Ejemplo
function getCookie(cname) {
    var name = cname + "=";
    var decodedCookie = decodeURIComponent(document.cookie);
    var ca = decodedCookie.split(';');
    for(var i = 0; i <ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') {
            c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
            return c.substring(name.length, c.length);
        }
    }
    return "";
}
*/









/*
Una función para verificar una cookie
Por último, creamos la función que verifica si se establece una cookie.

Si la cookie está configurada, mostrará un saludo.

Si la cookie no está configurada, mostrará un cuadro de aviso, solicitando el nombre del usuario, y almacenará la cookie de usuario durante 365 días, llamando a la función setCookie:

Ejemplo

function checkCookie() {
    var username = getCookie("username");
    if (username != "") {
        alert("Welcome again " + username);
    } else {
        username = prompt("Please enter your name:", "");
        if (username != "" && username != null) {
            setCookie("username", username, 365);
        }
    }
}


*/
















/*

Todos juntos ahora
Ejemplo*/

function setCookie(cname, cvalue, exdays) {
    var d = new Date();
    d.setTime(d.getTime() + (exdays * 24 * 60 * 60 * 1000));
    var expires = "expires="+d.toUTCString();
    document.cookie = cname + "=" + cvalue + ";" + expires + ";path=/";
}

function getCookie(cname) {
    var name = cname + "=";
    var ca = document.cookie.split(';');
    for(var i = 0; i < ca.length; i++) {
        var c = ca[i];
        while (c.charAt(0) == ' ') {
            c = c.substring(1);
        }
        if (c.indexOf(name) == 0) {
            return c.substring(name.length, c.length);
        }
    }
    return "";
}

function checkCookie() {
    var user = getCookie("nombredeusuario");
    if (user != "") {
        alert("Bienvenido  " + user);
    } else {
        user = prompt("por favor ingresa tu nombre:", "");
        if (user != "" && user != null) {
            setCookie("nombredeusuario", user, 365);
        }
    }
}